@model aspDatabase.Models.BookingRequest

@{
    ViewData["Title"] = "Confirm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Подтверждение бронирования</h1>

<p>Вы уверены, что хотите подтвердить бронирование для:</p>
<p>Имя: @Model.FirstName @Model.SecondName</p>
<p>Дата начала бронирования: @Model.BookingDateStart</p>
<p>Дата окончания бронирования: @Model.BookingDateEnd</p>

<form asp-action="ConfirmBooking" asp-route-id="@Model.Id" method="post">
    <button type="submit" class="btn btn-success">Подтвердить бронь</button>
    <a asp-action="Index" class="btn btn-secondary">Отмена</a>
</form>